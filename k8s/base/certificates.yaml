apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: admin@lesaiglesbraves.online
    privateKeySecretRef:
      name: letsencrypt-prod
    solvers:
    - http01:
        ingress:
          class: nginx
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ocis-tls-cert
  namespace: owncloud
spec:
  secretName: ocis-tls
  commonName: dev.lesaiglesbraves.online
  dnsNames:
    - dev.lesaiglesbraves.online
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: keycloak-tls-cert
  namespace: owncloud
spec:
  secretName: keycloak-tls
  commonName: dev.lesaiglesbraves.online
  dnsNames:
    - dev.lesaiglesbraves.online
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
